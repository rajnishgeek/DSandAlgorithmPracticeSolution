#include<bits/stdc++.h>
using namespace std;

class Graph
{
    public:
    map<int,vector<int>> *adj;
    void Graph()
    {
        adj=map<int,vector<int>>
    }
}
int main()
{
    Graph graph;
    addEdge(mapsvector, 0, 1);
    addEdge(mapsvector, 0, 4);
    addEdge(mapsvector, 1, 2);
    addEdge(mapsvector, 1, 3);
    addEdge(mapsvector, 1, 4);
    addEdge(mapsvector, 2, 3);
    addEdge(mapsvector, 3, 4);
    return 0;
}
